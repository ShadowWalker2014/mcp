# Stripe Configuration
# Required: Your Stripe secret key (get from https://dashboard.stripe.com/apikeys)
STRIPE_SECRET_KEY=sk_test_your_secret_key_here

# Optional: Your Stripe publishable key
STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# Optional: Your webhook endpoint secret (get from webhook endpoint settings)
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Note: Use test keys (sk_test_, pk_test_) during development
# Use live keys (sk_live_, pk_live_) only in production 